import streamlit as st
from PIL import Image
import os

# Relative path to plots folder
plot_folder = "plots"

# Dictionary of your plots
image_files = {
    "Crime by Year": "crime_by_year.png",
    "Heatmap": "heatmap.png",
    "Top Crimes": "top_crimes.png",
}

st.title("🚓 Police Data Dashboard")

selected_plot = st.sidebar.selectbox("Select a plot:", list(image_files.keys()))
image_path = os.path.join(plot_folder, image_files[selected_plot])

image = Image.open(image_path)
st.image(image, caption=selected_plot, use_column_width=True)































































